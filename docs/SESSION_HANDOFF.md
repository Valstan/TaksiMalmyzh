# Такси (TaksiMalmyzh) — Session Handoff

**Status:** ACTIVE — M0 стартован; гигиенические мандаты brain закрыты
**Updated:** 2026-08-25
**Branch:** main после merge PR этой сессии (#2, #3, #5, #6)

## Текущее состояние

Стадия M0 (пред-постройка): кода, БД и прода нет. Платформа решена владельцем
2026-07-23: **PWA, гео только foreground**. Хостинг выбран владельцем 2026-07-26;
детали размещения — в карточке проекта на brain-стороне, в этот публичный репозиторий
их не пишем (AGENTS.md §«Публичный репозиторий — recon-поверхность»). Ретеншн
гео-точек — гейт go-live.

## Сделано в сессии 2026-08-25

- **Мандат 08-04:** чужие репозитории больше не синхронизируются, входящий mailbox
  читается в два канала (локально + GitHub API) — переписаны `AGENTS.md` и `/start`;
  ack-письмо отправлено (PR #2).
- **Мандат 08-17 (secret-scan):** гейт стоит — `gitleaks-action@v3` + `checkout@v5`,
  `fetch-depth: 0`, `.gitleaks.toml` с правилом `taksi-apikey-uuid` (PR #3).
  Мутационная приёмка пройдена красным прогоном на двух случайных подсадных классах
  (PR #4 закрыт без слияния, ветка удалена). Имя job для required check — `gitleaks` —
  отправлено brain письмом.
- **Мандат D-038:** аудит всех tracked-файлов; инфра-детали вычищены из `AGENTS.md`,
  `PROJECT_STATE.md` и этого handoff; предохранитель переписан «через свойство, а не
  канал» (PR #5 + #6). G260-греп — чисто.
- **Ответ на kickoff (08-09):** «стартую, первый шаг — M0.A» + ack трёх мандатов
  26.07 и подписи автора. Все письма — в `mailbox/to-brain/`.
- Заведён `docs/GO_LIVE_CHECKLIST.md`; `docs/PROJECT_STATE.md` актуализирован под
  решения владельца (PWA foreground, серверная копия точек = часть MVP).

## Следующий шаг

**M0.A** — документ дизайна приватности и ретеншна геоданных (в `docs/`, затем в
`to-brain` до проектирования схемы БД): какие точки и с какой частотой; ретеншн явным
числом; что видит доверенный контакт; ключ извлечения трассы при потере телефона;
арифметика объёма на год. Параллельно **M0.B** — живой probe Яндекс Карты / 2ГИС
(лимиты, бесплатный тир, цена роста).

## Открытые вопросы владельцу

- Go на активную разработку Фазы 1 — отдельное решение после M0.

## Правило продолжения

Любая модель начинает с `AGENTS.md`, затем входящие brain (двухканально, чужие репо
только чтение) и этот handoff. Предыдущий чат не нужен для восстановления контекста.
