# Такси (TaksiMalmyzh) — Session Handoff

**Status:** ACTIVE — M0 выполнен, ждём решений владельца; мандаты brain закрыты
**Updated:** 2026-08-25
**Branch:** main после merge PR этой сессии (#2, #3, #5, #6, #7)

## Текущее состояние

Стадия M0 **выполнена по обоим трекам**: дизайн приватности написан, probe карт
выполнен. Кода, БД и прода по-прежнему нет — и следующий шаг упирается не в работу,
а в решения владельца (см. ниже). Платформа решена владельцем 2026-07-23: **PWA, гео
только foreground**. Хостинг выбран владельцем 2026-07-26; детали размещения — в
карточке проекта на brain-стороне, в этот публичный репозиторий их не пишем
(AGENTS.md §«Публичный репозиторий — recon-поверхность»).

## Сделано в сессии 2026-08-25

- **Мандат 08-04:** чужие репозитории больше не синхронизируются, входящий mailbox
  читается в два канала (локально + GitHub API) — переписаны `AGENTS.md` и `/start`;
  ack-письмо отправлено (PR #2).
- **Мандат 08-17 (secret-scan):** гейт стоит — `gitleaks-action@v3` + `checkout@v5`,
  `fetch-depth: 0`, `.gitleaks.toml` с правилом `taksi-apikey-uuid` (PR #3).
  Мутационная приёмка пройдена красным прогоном на двух случайных подсадных классах
  (PR #4 закрыт без слияния, ветка удалена). Имя job для required check — `gitleaks` —
  отправлено brain письмом.
- **Мандат D-038:** аудит всех tracked-файлов; инфра-детали вычищены из `AGENTS.md`,
  `PROJECT_STATE.md` и этого handoff; предохранитель переписан «через свойство, а не
  канал» (PR #5 + #6). G260-греп — чисто.
- **Ответ на kickoff (08-09):** «стартую, первый шаг — M0.A» + ack трёх мандатов
  26.07 и подписи автора. Все письма — в `mailbox/to-brain/`.
- Заведён `docs/GO_LIVE_CHECKLIST.md`; `docs/PROJECT_STATE.md` актуализирован под
  решения владельца (PWA foreground, серверная копия точек = часть MVP).
- **M0.A выполнен** — `docs/PRIVACY_GEODATA_DESIGN.md`. Главный результат:
  противоречие §3 концепта оказалось не про шифрование, а про **связывание** личности с
  псевдонимом; единственный работающий путь создаётся пассажиром **до** поездки
  отправкой ссылки. Отсюда: шаринг — часть старта, ссылка переживает поездку, E2E
  отвергнут (отказывает ровно в целевом сценарии). Политика записи, ретеншн
  30 суток + 12 мес, модель контакта, контур 152-ФЗ, злоупотребления.
- **M0.B выполнен** — `docs/PROBE_MAPS_PROVIDER.md`. Экономика не обнуляется, но
  лицензии Яндекса и 2ГИС **запрещают хранить** данные геокодера, что несовместимо с
  сутью продукта. Решение: MapLibre + собственные тайлы OSM + свой адресный справочник
  (внешних ключей и оферт не требует вообще).
- Замерами опровергнута посылка «ретеншн — гейт по ёмкости»: устойчивое состояние
  ≈ 38 МБ. Гейт остаётся, но по закону и приватности. brain отправлена поправка.

## Следующий шаг

Работы, не заблокированной решениями владельца, в M0 больше нет. Дальше — **решения
владельца** (`PRIVACY_GEODATA_DESIGN.md` §8) и **юридическая проверка** (§9).

Если владелец даст go до того, как закроются юридические вопросы, безопасный порядок
такой: сначала адресный справочник Малмыжа из OSM и карта на MapLibre (не касается ПДн
вообще), затем схема поездок и точек — но только после ответа на вопрос §9.1 (как
реализовать права субъекта ПДн без аккаунта), потому что он влияет на схему.

## Открытые вопросы владельцу

1. **Правовая форма оператора** (физлицо / самозанятый / ИП / ООО) — до подачи
   уведомления в РКН.
2. **Режим для несовершеннолетних**: родительский режим, формальное 18+ или комбинация.
3. **Срок хранения свёрнутой трассы**: 12 месяцев или 6.
4. **Бюджет на юриста по ПДн** — часть вопросов исследованием не закрывается.
5. **Явное принятие остаточного риска сталкинга** (§10 M0.A).
6. **Go на активную разработку Фазы 1.**

## Правило продолжения

Любая модель начинает с `AGENTS.md`, затем входящие brain (двухканально, чужие репо
только чтение) и этот handoff. Предыдущий чат не нужен для восстановления контекста.
