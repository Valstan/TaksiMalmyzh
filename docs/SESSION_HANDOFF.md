# Такси (TaksiMalmyzh) — Session Handoff

**Status:** PAUSED — M0 готов к старту, активная разработка не начата
**Updated:** 2026-07-30
**Branch:** main после merge PR этой сессии

## Текущее состояние

Проект остаётся на стадии концепта/M0: исходного кода, БД и работающего прода нет.
Решения владельца из brain учтены: клиент — PWA с гео только в foreground; будущий
хостинг — Бокс Sabantuy. Плановый домен существует в DNS, но приложение не развёрнуто.

## Сделано в сессии 2026-07-30

- Проведён аудит репозитория, актуальной карточки brain, домена и выбранного VPS.
- Введён `AGENTS.md` как общий источник правил для любых AI-агентов.
- `CLAUDE.md` превращён в тонкий адаптер; добавлен аналогичный `GEMINI.md`.
- Зафиксировано, какие AI-файлы коммитятся, а какие локальные данные игнорируются.
- Определён безопасный протокол параллельной работы: отдельные ветки/worktree, границы
  задач, сохранение чужих изменений, координация через Git/PR и handoff.
- В `mailbox/to-brain/` отправлено предложение сделать подход межпроектным стандартом.

## Следующий шаг

M0.A — документ дизайна приватности и ретеншна геоданных; M0.B — живой probe
Яндекс Карт/2ГИС. После M0 требуется отдельный go владельца на постройку Фазы 1.

## Открытые вопросы владельцу

- Go на активную разработку Фазы 1 после завершения M0.

## Правило продолжения

Любая модель начинает с `AGENTS.md`, затем проверяет входящие brain и этот handoff.
Предыдущий чат не считается необходимым для восстановления контекста.
