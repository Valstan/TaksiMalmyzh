# PROJECT_STATE — TaksiMalmyzh

**Статус:** M0 (пред-постройка). Платформа клиента **решена владельцем 2026-07-23**:
**PWA, геолокация только при открытом приложении (foreground)**. Хостинг **выбран
владельцем 2026-07-26** (детали размещения — в карточке проекта на brain-стороне; в
публичные файлы этого репозитория их не пишем). Кода, БД и прода ещё нет.

**Источник правды по стратегии:** [`../brain_matrica/docs/plans/taxi-safety-app-concept.md`](../brain_matrica/docs/plans/taxi-safety-app-concept.md).
Этот файл — рабочая выжимка для проектной сессии; при расхождении первичен концепт
Мозга **с поправками решений владельца 2026-07-23/26** (концепт писался в предположении
фонового GPS — оно снято, см. §4).

**Домен (план):** такси.вмалмыже.рф · **Карточка реестра:** [`../brain_matrica/projects/TaksiMalmyzh.md`](../brain_matrica/projects/TaksiMalmyzh.md)

---

## 1. Фазовое видение

Двусторонний маркетплейс через одностороннюю пользу: сперва решаем безопасность
пассажира и набираем спрос → потом онбордим предложение (таксопарки) → монетизируем
конкуренцию за видимость.

| Фаза | Что | Роль |
|---|---|---|
| **Ф1 — безопасность пассажира** | Пассажир включает поездку, приложение трекает маршрут как подстраховку («найти пропавшего»). Псевдонимно, без слежки, без аккаунта. | MVP, ядро роста |
| **Ф2 — таксопарки** | Таксопарки регистрируются (вход только через ЕСА/Радар-ID), предлагают услуги; заказы в приложении. | Онбординг предложения |
| **Ф3 — монетизация** | Фирмы конкурируют за показы (аукцион / featured-листинги). | Маркетплейс |

Ставка: односторонняя польза (безопасность) должна быть достаточно ценной **сама по
себе**, чтобы набрать аудиторию до появления таксопарков.

## 2. MVP Фазы 1 (строим первым)

1. **Запись поездки (foreground):** старт/стоп, гео-трасса (точки + таймстемпы), точки
   выезда/прибытия, дата/время. Screen Wake Lock на экране поездки; **явное состояние
   записи** («идёт запись» / «запись прервана»).
2. **Серверная копия точек — часть MVP, не «фаза 2»:** запись прерывиста by-design,
   единственная надёжная копия — серверная. Точки уходят **идемпотентными пачками**
   (повтор пачки не плодит дубли), незагруженное копится локально и досылается.
3. **Разрывы трассы — штатное состояние данных:** модель точек и отрисовка маршрута
   обязаны переживать их без «дырок, похожих на баг».
4. **Шаринг поездки с доверенным контактом** (что именно видит контакт — решается в
   M0.A) — главный механизм «найти/подстраховать».
5. **История поездок** пользователя; пользователь видит только свои поездки, контакт —
   только расшаренное (**server-side authz с первого дня**, #015).
6. **Псевдонимность by-design** — без аккаунта, без рекламной личности; foreground сам
   по себе исключает скрытую слежку (анти-цель концепта закрыта конструктивно).

## 3. Стек

| Слой | Состояние | Комментарий |
|---|---|---|
| **Клиент** | ✅ **Решено владельцем 2026-07-23: PWA, гео только foreground** | магазины приложений не нужны; 152-ФЗ проще (нет фонового сбора); Capacitor/APK не отвергнут — отложен до реальной бизнес-потребности в фоне |
| **Бэкенд + админка** | 🔶 предложение: Payload CMS + Next.js + PostgreSQL | стек, уже отработанный в экосистеме; утверждается на стадии постройки |
| **Карты** | 🔶 РФ-провайдер: Яндекс.Карты / 2ГИС | выбор — по результату живого probe (M0.B): лимиты, бесплатный тир, цена роста |
| **Хостинг** | ✅ выбран владельцем 2026-07-26 | ёмкость хранилища ограничена → ретеншн гео-точек = гейт go-live; сборка только в CI (G20) |

## 4. Ключевые риски

- **Foreground-трекинг честен, но хрупок:** экран гаснет — запись прерывается. Лечится
  Wake Lock + явным состоянием записи + серверной копией с досылкой (§2). Сценарий
  «сел в такси и убрал телефон» **не работает** — это проектируется и честно
  формулируется в интерфейсе, а не обнаруживается пользователем.
- **Приватность геоданных + 152-ФЗ.** Геолокация — чувствительные ПДн; даже
  «анонимный» трек может реидентифицировать. Явное согласие, минимизация, хранение в
  РФ. **Внутреннее противоречие** «псевдонимно» ↔ «трасса извлекаема, чтобы найти
  пропавшего» решается **дизайном** в M0.A: псевдонимность, доступ контакта, ключ
  извлечения, ретеншн/прореживание.
- **Ёмкость хранилища.** Гео-точки растут неограниченно, ёмкость хранилища конечна; разброс
  объёма ×30 в зависимости от частоты записи. Адаптивная частота + прореживание +
  ретеншн явным числом — **до** схемы БД (#084).
- **Two-sided cold-start.** Если безопасность не наберёт аудиторию сама по себе —
  предложение (таксопарки) не придёт.
- *(смежные)* Позиционирование «подстраховка/лог», не гарантия спасения; лимиты и
  стоимость карт-провайдера (может обнулить экономику — потому probe в M0).

## 5. Следующие шаги (M0 — до scaffold и схемы БД)

1. **M0.A — дизайн приватности и ретеншна геоданных** (письменный документ в `docs/`):
   какие точки и с какой частотой; ретеншн явным числом; что видит доверенный контакт;
   кто и по какому ключу извлекает трассу при потере телефона; арифметика объёма на
   год. Отправить в `to-brain` до проектирования схемы.
2. **M0.B — живой probe карт-провайдера** (Яндекс.Карты / 2ГИС): лимиты, бесплатный
   тир, гео-блок, цена роста. Не по докам — живым запросом (#020).
3. **Go владельца** на активную разработку Фазы 1 — отдельное решение после M0.
4. Затем: финализация бэкенд-стека, scaffold, Ф1 (чек-лист выката —
   [`GO_LIVE_CHECKLIST.md`](GO_LIVE_CHECKLIST.md)).

---

> **Граница ролей:** стратегия/концепт/реестр — в brain_matrica. Постройка/scaffold/
> деплой — в этой проектной сессии. Инфра-детали размещения (адреса, имена серверов,
> порты, соседи) в публичные файлы этого репозитория не пишем — см. `AGENTS.md`.
