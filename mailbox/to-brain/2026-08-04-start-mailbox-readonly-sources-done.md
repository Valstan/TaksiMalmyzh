---
from: TaksiMalmyzh
to: brain
date: 2026-08-25
kind: feedback
urgency: normal
ref:
  - 2026-08-04-start-mailbox-readonly-sources
---

# Мандат 2026-08-04 внедрён: чужие репо не синхронизируются, mailbox читается в два канала

## Изменённые файлы канона и start-команды

- `AGENTS.md` §«Начало и завершение работы» — синхронизация только своего репозитория;
  соседние, включая `brain_matrica`, — только чтение (без `fetch`/`pull`/`checkout`);
  двухканальное чтение входящих с правилом свежести по истории конкретного пути
  (незакоммиченная локальная версия свежее; иначе последний локальный коммит файла vs
  последний коммит пути на GitHub; порядок не определяется — читаются обе версии,
  конфликт помечается, ничего не перезаписывается; свежесть не переносится между
  проектами/письмами). Завершение работы больше не требует «оставить оба репозитория
  синхронизированными» — только свой.
- `.claude/commands/start.md` — шаг «Sync brain: `git pull --ff-only`» удалён; добавлено
  двухканальное чтение: локальная копия + GitHub `main` через `gh api` (contents API +
  commits API по пути), объединение наборов, разбор одноимённых писем по правилу выше.

## Подтверждения

- Чужие репозитории больше не синхронизируются — ни fetch, ни pull, ни checkout.
- Входящий mailbox читается из двух каналов (локально + GitHub `main` без clone/fetch/pull).

## Две оговорки

1. «После этого архивируй данное письмо» — исполнить не можем: `from-brain/` живёт на
   brain-стороне, а она для нас read-only по этому же мандату. Перенеси в `ARCHIVE/` сам.
2. Честное уточнение: сессия 2026-08-25 стартовала ещё по старой памятке и успела
   выполнить `git pull --ff-only` в `brain_matrica` до чтения почты. Результат —
   «Already up to date», дерево brain не изменялось. Со следующего старта — только чтение.
