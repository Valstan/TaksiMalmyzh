// Человеческие названия категорий справочника. Единственный экземпляр в продукте.
//
// Отдельный модуль, а не часть `lib/shelves.ts`, из-за границы сервер/клиент: подписи
// нужны и выпадашке подсказок в форме (клиентский компонент), а `lib/shelves.ts` держит
// `shelfCounts` с динамическим импортом Payload — сборщик клиентского бандла потянул бы за
// ним серверный код. Здесь ни импортов, ни побочных эффектов: только данные.

import type { EntryCategory } from "./sites.ts";

export const CATEGORY_LABELS: Record<EntryCategory, string> = {
  taxi: "Такси",
  shop: "Магазины",
  master: "Мастера и ремонт",
  brigade: "Бригады и работы",
  cargo: "Доставка и грузы",
  other: "Другое",
};
