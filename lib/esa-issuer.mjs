// Адрес единого входа экосистемы (ЕСА, `вход.вмалмыже.рф`) — один экземпляр строки.
//
// Он нужен в двух местах, которые не могут импортировать друг друга: модуль входа
// `lib/oidc.ts` (TypeScript, рантайм приложения) и `next.config.mjs` (обычный ESM, его
// грузит сам Next). Второму адрес нужен для директивы `form-action`: без хоста ЕСА в
// политике браузер режет редирект на выход, который делает форма «выйти» (см. конфиг).
//
// Кириллица в OAuth-полях не проходит (G108), поэтому строка в punycode. Модуль намеренно
// без зависимостей: его тянет конфиг Next до всего остального.

/** `https://вход.вмалмыже.рф` в punycode. */
export const DEFAULT_ISSUER = "https://xn--b1ae3a1a.xn--80adkdyec4j.xn--p1ai";
